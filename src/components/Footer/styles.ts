import { styled } from "@mui/system"

export const FooterContainer = styled("footer")(({ theme }) => ({
   position: "absolute",
   //width: "100%",
   paddingRight: "15px",
   paddingLeft: "15px",
   marginRight: "auto",
   marginLeft: "auto",
}))
